# Continuous Learning v2 - 快速上手指南

## 🎯 三个核心脚本的使用时机

### 1. observe.sh - 观察 Hook（已自动启动 ✅）

**状态**: ✅ **已配置完成，无需任何操作！**

**配置位置**: `hooks/hooks.json` 第 34 行和第 58 行

**自动触发时机**:
- 每次 Claude 使用工具**之前**（PreToolUse）
- 每次 Claude 使用工具**之后**（PostToolUse）

**工作内容**:
- 捕获工具名称、输入、输出
- 写入 `~/.claude/homunculus/observations.jsonl`
- 100% 可靠，不会遗漏

**验证是否工作**:
```bash
# 查看观察记录（使用插件一会儿后）
ls -lh ~/.claude/homunculus/observations.jsonl
tail -5 ~/.claude/homunculus/observations.jsonl
```

---

### 2. start-observer.sh - 后台分析进程（可选）

**状态**: ⏸️ **未启动（需要手动启动）**

#### ⏰ 何时启动？

| 时间点 | 是否启动 | 说明 |
|--------|---------|------|
| **刚安装** | ❌ 不需要 | 观察数据还不够，等待积累 |
| **使用 3-7 天** | ✅ 可以启动 | 已有一些观察数据 |
| **使用 2 周+** | ✅ 推荐启动 | 有足够数据进行模式分析 |

#### 🚀 如何启动？

```bash
# 1. 检查观察数据是否足够
wc -l ~/.claude/homunculus/observations.jsonl
# 建议 > 50 行再启动

# 2. 启动 observer（后台运行）
cd /Users/siting/work/project/self/claude/blacklake-claude-marketplace/plugins/self-assistant-plugin
bash skills/continuous-learning-v2/agents/start-observer.sh start

# 3. 查看状态
bash skills/continuous-learning-v2/agents/start-observer.sh status

# 4. 查看日志
tail -f ~/.claude/homunculus/observer.log

# 5. 停止（如果不需要了）
bash skills/continuous-learning-v2/agents/start-observer.sh stop
```

#### 🔄 工作机制

启动后会：
1. **每 5 分钟**检查 observations.jsonl
2. 如果有 10+ 条新记录，调用 `claude --model haiku` 分析
3. 检测到明确模式（3+ 次重复）时**自动创建本能文件**
4. 保存到 `~/.claude/homunculus/instincts/personal/`
5. 归档已处理的观察记录

#### 💡 要不要启动？

**启动的优点**:
- ✅ 完全自动化，无需手动分析
- ✅ 定期生成本能，持续学习
- ✅ 后台运行，不影响使用

**不启动也可以**:
- ⚠️ 观察数据会继续积累
- ⚠️ 可以手动查看 observations.jsonl
- ⚠️ 需要时手动运行 `/instinct-status`
- ⚠️ 节省系统资源

**推荐策略**: 使用 2 周后启动，让系统自动学习

---

### 3. instinct-cli.py - 本能管理工具（按需使用）

**状态**: 💤 **按需执行，不是后台服务**

这不是需要"启动"的服务，而是一个命令行工具，需要时执行即可。

#### 📋 可用命令

| 命令 | 何时使用 | 使用频率 |
|------|---------|---------|
| `/instinct-status` | 想查看学到了哪些本能 | 建议：每周一次 |
| `/instinct-export` | 想分享本能给团队 | 需要时 |
| `/instinct-import <file>` | 导入他人的本能 | 新加入团队时 |
| `/evolve` | 将多个本能合并为技能 | 有 3+ 相关本能时 |

#### 🎯 具体使用场景

**1. 查看学习进度**
```bash
# 时机：每周查看一次
/instinct-status

# 输出示例：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 学习的本能列表

1. prefer-grep-before-edit (置信度: 0.65)
   触发: 当需要搜索代码进行修改时
   行动: 先使用 Grep 找到精确位置

2. always-test-first (置信度: 0.80)
   触发: 当添加新功能时
   行动: 先编写测试用例

总计: 2 个本能
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**2. 导出分享**
```bash
# 时机：想分享给团队成员
/instinct-export

# 输出：~/.claude/homunculus/instincts-export-20260205.json
```

**3. 导入团队经验**
```bash
# 时机：新加入团队或迁移机器
/instinct-import team-instincts.json
```

**4. 演化本能**
```bash
# 时机：有多个相关本能时（如 3 个关于测试的本能）
/evolve

# 系统会自动：
# 1. 分析本能相似度
# 2. 聚类相关本能
# 3. 生成完整的技能文件
```

---

## 📅 推荐使用时间线

### 第 1-7 天：数据积累期
```
✅ observe.sh 自动运行（已配置）
❌ 不启动 start-observer.sh
❌ 不使用 instinct-cli.py

目标：积累观察数据
操作：正常使用 Claude Code 即可
```

### 第 7-14 天：启动自动分析
```bash
# 1. 检查数据量
wc -l ~/.claude/homunculus/observations.jsonl
# 建议 > 50 行

# 2. 启动 observer
bash skills/continuous-learning-v2/agents/start-observer.sh start

# 3. 继续正常使用
# observer 会在后台自动分析
```

### 第 14 天+：定期管理
```bash
# 每周查看一次
/instinct-status

# 发现有用的本能时
/instinct-export

# 发现相关本能时
/evolve
```

---

## 🎯 快速决策流程图

```
刚安装插件
    ↓
observe.sh 已自动配置 ✅
    ↓
正常使用 Claude Code（1-2 周）
    ↓
检查：observations.jsonl 是否 > 50 行？
    ├─ 是 → 启动 start-observer.sh ✅
    │        ↓
    │        每周执行 /instinct-status 查看进度
    │        ↓
    │        发现有用本能时 → /instinct-export
    │        ↓
    │        有 3+ 相关本能时 → /evolve
    │
    └─ 否 → 继续使用，等待数据积累
```

---

## 💡 核心要点总结

### observe.sh（最重要）
- ✅ **已自动配置**，无需任何操作
- ✅ 每次工具使用时自动触发
- ✅ 数据自动保存到 observations.jsonl

### start-observer.sh（可选）
- ⏰ **使用 1-2 周后**再启动
- 🎯 后台自动分析，创建本能
- 💰 使用 Haiku 模型节省成本

### instinct-cli.py（按需使用）
- 📊 **每周执行** `/instinct-status` 查看进度
- 📤 需要分享时执行 `/instinct-export`
- 🔄 有相关本能时执行 `/evolve`

---

## ❓ 常见问题

### Q1: 我需要做什么才能开始学习？
**A**: 什么都不需要！observe.sh 已经配置好，会自动记录。

### Q2: 多久能看到学习效果？
**A**: 
- 观察数据：立即开始积累
- 自动本能：启动 observer 后 1-2 周
- 手动查看：随时可以查看 observations.jsonl

### Q3: start-observer.sh 必须启动吗？
**A**: 不必须。不启动也会记录数据，只是不会自动创建本能。

### Q4: 会占用很多资源吗？
**A**: 
- observe.sh：几乎不占用资源（每次 < 100ms）
- start-observer.sh：轻量级（每 5 分钟分析一次）
- 使用 Haiku 模型节省成本

### Q5: 中文本能文件有效吗？
**A**: 完全有效！Claude 对中文理解与英文相同。

### Q6: 如何验证系统在工作？
**A**: 
```bash
# 查看观察记录
cat ~/.claude/homunculus/observations.jsonl

# 查看学到的本能
/instinct-status

# 查看 observer 状态
bash skills/continuous-learning-v2/agents/start-observer.sh status
```

---

## 🚀 现在就开始使用！

**你只需要**：
1. ✅ 正常使用 Claude Code（observe.sh 已自动工作）
2. ⏰ 1-2 周后启动 start-observer.sh（可选）
3. 📊 定期查看 /instinct-status（可选）

就这么简单！系统会自动学习你的使用模式。🎉
